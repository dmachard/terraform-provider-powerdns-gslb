name: Testing

on:
  push:
    branches:
      - main
      
jobs:
  release:
    runs-on: ubuntu-20.04
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-go@v1
      with:
        go-version: '1.15'
    - name: Install pdns-server
      run: |
        sudo docker run -d -p 5353:53/udp -p 5353:53/tcp --name=powerdns powerdns/pdns-auth-44
        sudo docker logs powerdns